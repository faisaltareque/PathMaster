from setuptools import setup, find_packages

setup(
    name='pathmaster',
    version='0.1.0',
    description='A simple utility to add directories to the sys.path in notebooks.',
    author='Faisal Tareque',
    packages=find_packages(),
    install_requires=[],
)