from setuptools import setup, find_packages

setup(
    name='pathmaster',
    version='0.1.1',
    description='A simple utility to work with directories.',
    author='Faisal Tareque',
    packages=find_packages(),
    install_requires=[],
)